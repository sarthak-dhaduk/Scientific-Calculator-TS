{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "if (e.target.title == \"Eql\") {             try {                 display = eval(display);                 displayOT.textContent = display;             }             catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.id == \"c\") {             display = \"\";             displayOT.textContent = display;         }         else if (e.target.title == \"d-1\") {             display = display.slice(0, display.length - 1)             displayOT.textContent = display;         }         else if (e.target.title == \"x2\") {             try {                 display = display + \"*\" + display;                 display = eval(display);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"x3\") {             try {                 display = display + \"**3\";                 display = eval(display);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"sqrt\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.sqrt(subDisplay)                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"cbrt\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.cbrt(subDisplay)                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"1/\") {             try {                 display = \"1/\" + display;                 display = eval(display);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"log\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.log10(subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"in\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.log(subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"mx\") {             if (Number(display) < 0) {                 display = display.slice(display.length - 1);                 displayOT.textContent = display;             } else {                 displayOT.textContent = display;             }         }         else if (e.target.title == \"mp\") {             let num: number = Number(display);             if (num > 0) {                 display = display + \"*-1\";                 display = eval(display);                 displayOT.textContent = display;             } else {                 display = display + \"*-1\"                 display = eval(display);                 displayOT.textContent = display;             }         }         else if (e.target.title == \"exp\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.exp(subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"f-e\") {             try {                 display = Number.parseFloat(display).toExponential();                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"frr\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.round(subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"floor\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.floor(subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"rand\") {             try {                 subDisplay = Number(display);                 subDisplay = Math.floor(Math.random() * subDisplay);                 display = String(subDisplay);                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"10x\") {             try {                 display = \"10**\" + display;                 display = eval(display)                 displayOT.textContent = display;             } catch (err: unknown) {                 errMsg.textContent = \"Invalid Entry!\";                 setTimeout(() => {                     errMsg.textContent = \"\";                     displayOT.textContent = \"\";                     display = \"\";                 }, 3000);             }         }         else if (e.target.title == \"!\") {             for (i = 1; i <= Number(display); i++) {                 fact = fact * i;             }             displayOT.textContent = fact;             fact = 1;             i = 1;         }         else if (e.target.title == \"dms\") {             try {                 subDisplay = Number(display);                 let d: number = Math.floor(subDisplay);                 let minfloat: number = (subDisplay - d) * 60;                 let m: number = Math.floor(minfloat);                 let secfloat: number = (minfloat - m) * 60;                 let s: number = Math.round(secfloat);",
  "main": "index.js",
  "scripts": {
    "build": "webpack"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "webpack": "^5.76.3",
    "typescript": "^4.9.5", 
    "ts-loader": "^9.4.2"
  },
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}